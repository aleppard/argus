////////////////////////////////////////////////////////////////////////////////
package com.argus;

import org.junit.Test;

/**
 * Tests QueryEngine class.
 */
public class QueryEngineTest {

    /** 
     * Test that we can initialise the QueryEngine. We should
     * do an end-to-end test to actually test this class.
     */
    @Test public void test() {
        QueryEngine engine = new QueryEngine();
    }
}
